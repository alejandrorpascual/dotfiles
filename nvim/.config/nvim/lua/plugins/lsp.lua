return {
    "neovim/nvim-lspconfig",
    event = { "BufReadPre", "BufNewFile" }
    -- main = "config.lsp",
    -- opts = {
    --     mason = {
    --         enable = true,
    --         auto_install = true,
    --     },
    -- },
}
